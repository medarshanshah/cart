{
    "swagger": "2.0",
    "info": {
      "version": "1.0.0",
      "title": "Cart Service",
      "description": "cart service API",
      "license": {
        "name": "MIT",
        "url": "https://opensource.org/licenses/MIT"
      }
    },
    "host": "localhost:7000",
    "basePath": "/",
    "tags": [
      {
        "name": "Cart",
        "description": "API for cart in the system"
      }
    ],
    "schemes": ["http"],
    "consumes": ["application/json"],
    "produces": ["application/json"],
    
    "paths": {
      "/cart": {
        "get": {
          "tags": ["Cart"],
          "summary": "Get the cart of the current logged in User",
          "produces": ["application/json"],
          "responses": {
            "200": {
              "description": "OK",
              "schema": {
                "$ref": "#/definitions/Cart"
              }
            }
          }
        },

        "post": {
            "tags": ["Cart"],
            "description": "Request to add product into cart",
            "operationId": "post",
            "parameters": [
              {
                "name": "productId",
                "in": "body",
                "description": "Request to add product into cart",
                "schema": {
                  "$ref": "#/definitions/CartItem"
                }
              }
            ],
            "consumes": ["application/json"],
            "produces": ["application/json"],
            "responses": {
              "200": {
                "description": "New user is created",
                "schema": {
                  "$ref": "#/definitions/CartItem"
                }
              }
            }
          }

      }
    },
    
    "definitions": {
      "Cart": {
        "required": ["userId"],
        "properties": {
          "_id": {
            "type": "string",
            "uniqueItems": true
          },
          "userId": {
            "type": "string",
            "uniqueItems": true
          },
          "cartItems":{
              "type": "array",
              "$ref": "#/definitions/CartItem"
          },
          "cartEmpty": {
            "type": "boolean"
          }
        },
      "CartItem": {
        "type": "object",
        "required": ["productId"],
        "properties": {
          "_id": {
            "type": "string",
            "uniqueItems": true
          },
          "productId": {
            "type": "string",
            "uniqueItems": true
          },
          "cartId":{
              "type": "string"
          },
          "quantity": {
            "type": "integer"
          }
        }
      }
     }
   }
}




  
  